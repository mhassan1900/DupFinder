This is release 1.0.0 of DupFinder.
It finds duplicate files (based on hashsum signatures) from a given set of directories.  

There are both GUI & command line modes:   
    scripts/dupfinder.py          - command line version
    scripts/dupfinder_gui.py      - GUI version

